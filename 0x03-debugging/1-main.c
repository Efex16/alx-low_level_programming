#include <stdio.h>

/**
 * main - causes on infinite loop
 *
 * Return: 0 (successful)
 */
int main(void)
{
	int i;

	printf("infinte loop incoming :(\n");
	/**
	 * i = 0;
	 *
	 * while (i < 0)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("infinite loop avoided! \\o/\n");

	return (0);
}
